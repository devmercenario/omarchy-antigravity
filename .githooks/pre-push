#!/bin/bash
# Pre-push hook: run the marketplace preflight before pushing to any remote.
# Skip with `git push --no-verify`.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
echo "▶ Running pre-push preflight (tests/preflight.sh)..."
bash "$ROOT/tests/preflight.sh"
